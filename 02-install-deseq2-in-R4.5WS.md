# R
To start R statistical Programming language type R in the command line of the R4.W5S conda enviroment 

```{CMD}
R
```

# Install R packages for Bioconductor
Installing `BiocManager` package from CRAN( Comprehensive R Achhival Network) using `install.packages()` function in R base. Please type the following, 
```{R}
install.packages("BiocManager")
```